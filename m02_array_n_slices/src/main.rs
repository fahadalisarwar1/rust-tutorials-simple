fn main() {
    let arr: [i32; 3] = [10, 20, 40];
    let first = arr[0];
    println!("{}", first);
    for i in 0..=2{
        println!("{}", arr[i]);
    }
    println!("{}", arr.len());
    // println!("{}", arr[5]);

    // slices
    
}
